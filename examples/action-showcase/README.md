# Action Showcase

Run `npm run build`, then `npm run example:serve`. Open `http://127.0.0.1:4173/examples/action-showcase/`.

The canonical manifest exercises every action family through the same runtime used by Projector, Studio, CLI Test, and Capture.
